diff --git a/assign4/coreutils-with-bug/lib/timespec.h b/assign4/coreutils-with-bug/lib/timespec.h
index 9c630b8..73a805d 100644
--- a/assign4/coreutils-with-bug/lib/timespec.h
+++ b/assign4/coreutils-with-bug/lib/timespec.h
@@ -45,8 +45,15 @@ struct timespec
 static inline int
 timespec_cmp (struct timespec a, struct timespec b)
 {
-  int diff = a.tv_sec - b.tv_sec;
-  return diff ? diff : a.tv_nsec - b.tv_nsec;
+  if (a.tv_sec < b.tv_sec) {
+      return -1;
+  }
+  else if (a.tv_sec > b.tv_sec) {
+      return 1;
+  }
+  else {
+      return (a.tv_nsec - b.tv_nsec);
+  }
 }
 
 # if ! HAVE_DECL_NANOSLEEP
diff --git a/assign4/lab4.diff b/assign4/lab4.diff
index 03cb826..e69de29 100644
--- a/assign4/lab4.diff
+++ b/assign4/lab4.diff
@@ -1,17 +0,0 @@
---- lib/timespec.h
-+++ lib/timespec.h
-@@ -45,8 +45,12 @@ struct timespec
- static inline int
- timespec_cmp (struct timespec a, struct timespec b)
- {
--  int diff = a.tv_sec - b.tv_sec;
--  return diff ? diff : a.tv_nsec - b.tv_nsec;
-+  if (a.tv_sec < b.tv_sec):
-+      return -1;
-+  else if (a.tv_sec > b.tv_sec):
-+      return 1;
-+  else:
-+      return (a.tv_nsec - b.tv_nsec);
- }
- 
- # if ! HAVE_DECL_NANOSLEEP
